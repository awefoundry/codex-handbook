# 检查清单

1. 文件检查：`index.html`、`styles.css` 和本说明文件都存在。
2. 差异检查：运行 `git diff -- index.html styles.css`（若目录不是 Git 仓库，可直接比较起始文件和当前文件）。
3. 链接检查：确认按钮仍指向 `https://example.com`。
4. 浏览器检查：直接打开 `index.html`，确认标题、简介、按钮和样式。
5. 范围检查：只有标题和简介两处文字改变。
