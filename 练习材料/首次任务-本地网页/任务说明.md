# 首次任务：修改本地网页

## 目标

把 `index.html` 的主标题改为“我的第一个 Codex 练习”，把简介改为“先看清修改，再确认页面结果。”。

请保留按钮文字、`https://example.com` 链接、HTML 结构和全部 CSS 样式。

## 起点

只复制或解压本目录中的 `起始文件/` 到新的测试目录，再在 Codex App 或 CLI 中打开这个测试目录。不要直接修改本目录里的原始 starter。

## 验收

- [ ] 主标题已改成指定文字。
- [ ] 简介已改成指定文字。
- [ ] 按钮文字和链接仍为“查看阅读清单”和 `https://example.com`。
- [ ] `styles.css` 没有变化。
- [ ] 用浏览器打开 `index.html` 后，标题和简介显示正确，样式仍存在。
- [ ] 查看 diff，确认只有 `index.html` 中的两处文字变化。

## 参考差异

```diff
-    <h1>晨间读书角</h1>
-    <p class="intro">用十分钟记录今天读到的一段话，慢慢建立自己的阅读清单。</p>
+    <h1>我的第一个 Codex 练习</h1>
+    <p class="intro">先看清修改，再确认页面结果。</p>
```

## 重新开始

停止使用当前测试目录，重新从本目录的 `起始文件/` 解压或复制到一个新的目录。不要清空真实工作区，也不要执行破坏性 Git 重置。
