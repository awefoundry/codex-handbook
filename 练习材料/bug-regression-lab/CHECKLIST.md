# 完成检查

- [ ] `node --version` 已确认，且运行命令位于练习副本根目录。
- [ ] 起始测试在未修代码上通过，复现命令显示 `A-101: ¥NaN`。
- [ ] 新增的缺失值断言在未修代码上因为实际输出错误而失败。
- [ ] 没有删除或放宽 `12.5` 和 `0` 的正确断言。
- [ ] 修复后同一组测试通过，复现命令显示 `A-100: ¥12.50` 和 `A-101: --`。
- [ ] `git diff --check`（如果你为练习副本初始化了 Git）没有输出；没有 Git 时已逐项比较文件。
- [ ] 修改范围只有 `src/formatAmount.mjs` 和 `test/formatAmount.test.mjs`。
- [ ] 在另一份临时副本中把最终测试配回原始实现，回归断言仍然失败。
- [ ] 没有把 `reference/` 文件复制回起始目录，也没有把故意失败的测试放进网站工程。
