# CodexGuide 项目交接文档

最后更新：2026-09-04

本文用于交接 CodexGuide 的内容仓库、教程网站、文章上线、图片素材、搜索优化和日常维护流程。文中“已完成”只表示已有文件或记录支持的状态；没有经过远端、构建或线上检查的内容，不视为已发布。

## 一、项目概况

CodexGuide 面向中文 Codex 新手和开发者，提供基于官方资料、真实操作记录和可复核结果的教程。内容重点包括：

- Codex App、CLI、IDE 和 Cloud 的选择与安装。
- 任务描述、上下文、计划、权限、沙箱和审批。
- 项目理解、代码修改、Bug 修复、测试和验证。
- Git、分支、Worktree、Commit、Pull Request 和 Code Review。
- Skills、插件、MCP、自动化和高级工作流。
- 真实案例、故障排查、速查表和学习路线。

项目的内容基线是“准确、可复现、能说明验证方法”。文章不能只给出看起来合理的步骤，还要说明适用环境、限制、来源和完成标准。

## 二、两个仓库的职责

### 1. 教程仓库

- 仓库：`awefoundry/codex-handbook`
- 本地目录：`D:\codexguide_all\教程`
- 作用：保存教程正文、文章目录、图片素材、来源记录、学习路线和内容工作流资料。
- 在线地址：`https://github.com/awefoundry/codex-handbook`

主要入口：

| 文件或目录 | 作用 |
|---|---|
| `README.md` | 仓库入口、栏目说明和内容原则 |
| `学习路线.md` | 按读者目标组织阅读顺序 |
| `内容地图.md` | 按问题查找栏目 |
| `贡献指南.md` | 文章、来源、图片和链接规范 |
| `更新日志.md` | 仓库结构与内容的重要变更 |
| `TODO.md` | 当前任务、处理记录和待办 |
| `资料来源与版本记录/` | 官方资料、社区资料和核验记录 |
| `图片素材/` | 正式文章使用的图片 |
| `图片备份/` | 可追溯的原图、处理记录和历史素材 |
| `00-从这里开始/` 至 `16-图解与速查/` | 正文栏目 |
| `skills/` | 内容处理、封面或其他可复用 Skill |

### 2. 网站仓库

- 仓库：`chengyoushu/codexguide`
- 本地目录：`D:\codexguide_all\网站`
- 作用：将已确认的教程内容整理成可搜索、可导航、适合手机阅读的在线站点。
- 正式地址：`https://codexguide.io`
- 在线仓库地址：`https://github.com/chengyoushu/codexguide`

网站不会自动从教程仓库读取最新 Markdown。文章上线时，需要把教程终稿人工同步到网站的内容数据文件，并把正文图片复制到网站静态资源目录。

## 三、内容目录与栏目状态

网站当前以 `content/sections.ts` 定义栏目，以 `content/guides.ts` 定义正式教程，以 `content/cases.ts` 定义真实案例，以 `content/advanced.ts`、`content/faq.ts` 和 `content/quickstarts.ts` 定义专题页面内容。

教程仓库的一级栏目按学习顺序排列：

| 编号 | 栏目 | 内容定位 |
|---|---|---|
| 00 | 从这里开始 | 定位、适用人群、能力边界和入口选择 |
| 01 | 安装与首次使用 | App、CLI、IDE、登录和第一次任务 |
| 02 | 核心概念与任务方法 | 上下文、提示词、任务拆分和纠偏 |
| 03 | 项目理解与上下文 | `AGENTS.md`、项目结构、入口和依赖调用链 |
| 04 | 代码修改与开发实战 | 功能、Bug、重构、依赖和前端修改 |
| 05 | Git 协作与代码审查 | 分支、提交、PR、Review 和合并 |
| 06 | 测试调试与质量保障 | 测试、日志、排错和上线前验收 |
| 07 | Skills 实战 | Skill 的安装、创建、测试和复用 |
| 08 | 插件工作流 | Notion、GitHub 和其他外部插件 |
| 09 | MCP 与外部工具 | 外部数据源、服务器、工具权限和数据边界 |
| 10 | 配置模型与权限安全 | 模型、`config.toml`、沙箱、审批和网络 |
| 11 | 自动化与高级工作流 | 定时任务、长任务、并行任务和 CI |
| 12 | 官方工具与集成 | OpenAI 官方工具和集成方式 |
| 13 | 社区生态与项目评测 | 第三方项目、生态和方案判断 |
| 14 | 真实案例复盘 | 从需求到验证、交付和复盘的完整案例 |
| 15 | 故障排查与常见问题 | 安装、登录、权限、Git 和环境问题 |
| 16 | 图解与速查 | 关系图、命令表和检查清单 |

栏目是否进入网站公开导航，以网站 `content/sections.ts` 的 `status` 为准。带有“未发布”“待编写”“待实操”的文章不能进入公开教程列表、搜索、Sitemap 或 LLM 索引。

## 四、文章从选题到上线的完整流程

### 第 1 步：确定主题和交付对象

先写清楚文章服务的读者和问题，不要从“想介绍一个功能”开始。至少确定：

- 读者是谁。
- 读者遇到的具体问题是什么。
- 文章最终要让读者完成什么动作。
- 哪些步骤可以被复现。
- 怎样判断完成。
- 哪些内容属于当前版本，哪些内容需要后续复查。

文章文件名使用“序号-清晰标题.md”，避免使用“最终版”“最新版”“文章 1”等无法维护的名称。同一篇文章只保留一个正式位置，其他栏目通过链接引用。

### 第 2 步：核对官方资料和版本

产品功能、模型、套餐、价格、安装命令、配置字段、平台支持和界面位置都可能变化。写入正文前重新核对，不直接依赖旧文章或模型记忆。

优先级：

1. OpenAI 官方文档、官方仓库和官方产品页面。
2. 项目作者仓库、正式发布说明和原始技术文章。
3. 社区文章、视频和社交平台，仅用于选题或补充表达。

文章应记录官方页面、核对日期、适用版本、本地环境和已经验证的内容。无法验证的步骤要写成“待验证”或“可能因版本不同而变化”。

### 第 3 步：准备实操和证据

实操类文章至少要有入口、操作和结果三类证据；排障类文章要保留问题状态、判断过程、修复动作和修复后的结果。

截图前先准备：

- 干净或可丢弃的演示项目。
- 可重复的命令和操作步骤。
- 文章需要证明的关键信息。
- 截图中的脱敏范围。
- 失败时的停止点和回滚方式。

不要把 AI 生成的界面图当作真实运行结果。抽象概念可以使用 AI 插图，但必须在内部记录用途，不能误导读者。

### 第 4 步：写作和自然化复核

正文至少包含：适合谁、解决什么问题、前置条件、操作步骤或概念说明、成功判断、常见问题或风险、来源。

写作复核重点：

- 删除内部工作区路径、任务 ID、账号、Token、Cookie 和编辑过程。
- 删除“待处理素材”“备用配图”“截图方案”等内部制作内容。
- 不把推广内容写成产品事实，不把第三方写成官方功能。
- 避免空泛结论、模糊归因、过度宣传和重复段落。
- 图片按首次出现顺序编号，图注与图片位置保持一致。
- 链接文字说明目标，不使用大量无上下文的裸链接。

### 第 5 步：文章和素材验收

交付前逐项检查：

- Markdown 语法、标题层级和代码块闭合。
- 图片引用全部存在，文件名清晰，图号连续。
- 图片没有个人路径、邮箱、用户名、二维码、密钥或私有项目内容。
- 官方来源链接可访问，第三方来源有明确标注。
- 正文没有内部流程残留。
- `git diff --check` 通过。
- 文章的环境、版本和“最后验证”日期与当前证据一致。

文章专属图片工作区通常包含 `environment.md`、`manifest.tsv`、`manual-steps.md`、`research-log.md`、`gzhwz-processing-report.md` 以及原图和处理图。它们用于追溯，不应直接混入正式正文或网站公开内容。

## 五、图片素材管理

正式文章图片放在：

```text
图片素材/<章节目录>/<文章文件名（不含 .md）>/
```

命名建议：

```text
01-官方下载页面.png
02-登录完成.png
03-测试结果.png
```

不要使用空格、`image`、`截图`、UUID 或时间戳作为正式素材文件名。原图和中间产物放在文章旁边的图片备份目录，保留来源和处理映射。

图片选择顺序：

1. 官方真实界面截图。
2. 维护者在可复现环境中的实操截图。
3. 有明确用途的概念图。
4. 无法增加解释力时不配图。

处理图片时保留主体内容，不重绘真实界面来伪造运行结果。需要使用 `imagegzh` 或其他处理脚本时，记录原图、输出图、尺寸、处理方式和验收结果。

### 公众号内容工作流 Skill 与排版工具

当前负责的公众号为「日常学Python」。公众号账号的后台权限、登录资料和发布确认由负责人自行保管与操作。

当前公众号草稿箱中的文章均未发布，属于待后续确认的草稿内容。后续处理时应逐篇核对标题、正文、配图、排版、链接和发布状态，再决定是否提交预览或发布。

公众号文章相关 Skill 统一放在教程仓库的 `skills/` 目录，入口索引见 [`skills/README.md`](skills/README.md)。当前交接副本包括：

- `skills/gzhstart/`：创建文章旁的图片备份和素材研究工作区，记录环境、来源、截图清单、人工补拍步骤和校验结果。
- `skills/gzhwz/`：负责中文文章润色、正文配图处理、图片编号、链接埋点、CTA、脱敏验收和封面标题确认闸门。
- `skills/imagegzh/`：按统一的一号样式整理公众号正文截图，保留原图并输出新文件。
- `skills/humanize-zh-prose/`、`skills/human-writing/`、`skills/humanizer-zh/`：负责中文表达、事实边界和 AI 痕迹复核。
- `skills/research-article-visuals/`、`skills/agent-reach/`、`skills/playwright/`：负责素材检索、平台路由、公共网页核验和截图说明。
- `skills/baoyu-cover-image/`、`skills/banner-design/`、`skills/design/`：负责封面方向、横版尺寸、安全区和视觉复核，其中 `design` 只同步公开主文档。

使用这些 Skill 时通过 Skill 名称或 `$skill-name` 引用，不依赖 `C:\Users\...`、`D:\CodexHome\...` 等本机绝对路径。仓库副本不包含二维码、个人公众号历史文章白名单、后台截图、缓存、内部图片生成线路、Cookie、Token、API Key、AppSecret 或其他账号资料。`gzhwz/references/historical-article-catalog.md` 是公开空白模板，真实历史文章标题只保留在受控本地副本中。

公众号正文排版使用 [Markdown Nice](https://editor.mdnice.com/)。建议顺序是：先在本地完成正文和图片验收，再将 Markdown 粘贴到 Markdown Nice，检查标题层级、图片显示、图注、段间距、链接和手机端预览；确认格式无误后再由负责人手动处理公众号后台的预览、发布或群发。Markdown Nice 只作为排版工具记录，公众号发布操作仍由本人补充。

### 飞书机器人运行位置与密钥维护

CodexGuide 群内使用的飞书机器人运行在小媛姐的 Mac mini 上。后续如遇 API Key 过期、轮换或服务报错，应先在该 Mac mini 上确认机器人进程、启动方式和受控环境配置，再更新对应 API Key，并重启或重新加载机器人后做一条最小化消息验证。API Key 只保存在受控环境变量或本机密钥配置中，不写入教程仓库、交接文档、飞书公开留痕、日志或截图；交接时只记录更新日期、影响的机器人和验证结果。

## 六、教程仓库到网站仓库的同步

正式同步的固定顺序：

1. 在教程仓库完成文章和图片。
2. 检查教程仓库状态、差异、图片和敏感信息。
3. 将教程仓库终稿提交并推送到远端。
4. 确认远端分支已经收到提交。
5. 再在网站仓库更新 `content/guides.ts` 或对应内容数据文件。
6. 将网站实际使用的正文图片复制到 `public/tutorial-images/`。
7. 本地运行网站验证和页面检查。
8. 只提交本次网站同步涉及的文件。
9. 通过 `main` 工作流部署并检查线上页面。

网站文章对象通常需要填写：

- `slug`
- 标题和摘要
- `markdown`
- `sourcePath`
- `sourceUrl`
- 作者和贡献者
- 发布日期和修改日期
- 可选的 CLI 版本
- `position`

`sourcePath` 必须指向教程仓库中的真实 Markdown 文件。上一篇和下一篇由文章注册表的 `position` 计算，不要在多处手写导航关系。文章位置变化时，同时检查目录、搜索、Sitemap、相关文章和分页。

网站正文与公众号稿不是同一份展示格式。网站版本通常需要移除公众号封面、二维码、联系方式、引流文案和重复图注，只保留适合网页阅读的正文和必要图片。

## 七、网站结构和关键入口

网站使用 React、Next 兼容路由、Vite/vinext、TypeScript 和 Cloudflare Worker。

重要目录：

| 路径 | 作用 |
|---|---|
| `app/` | 页面、路由、布局、交互组件和 Markdown 渲染 |
| `content/` | 教程、案例、进阶专题、FAQ、快速上手和搜索数据 |
| `public/` | 静态图片、图标和发布资源 |
| `scripts/` | 构建前处理、SEO 检查和 IndexNow 脚本 |
| `worker/` | Cloudflare Worker 入口和 API 路由 |
| `tests/` | 构建后 HTML 渲染回归测试 |
| `db/`、`drizzle/` | D1/Drizzle 相关结构和迁移产物 |
| `.openai/hosting.json` | Sites/本地绑定所需的非敏感项目配置 |
| `.github/workflows/` | GitHub Actions 发布流程 |

主要公开入口：

- `/`：首页。
- `/guides`：教程目录。
- `/guides/[slug]`：教程详情页。
- `/quickstarts`：任务型快速上手。
- `/paths`：学习路线。
- `/advanced`：进阶专题。
- `/compare`：入口和能力比较。
- `/cases`：真实案例。
- `/guides/faq`：故障排查 FAQ。
- `/changelog`：网站更新日志。
- `/llms.txt`、`/llms-full.txt`：面向 AI 检索的内容索引。
- `/robots.txt`、`/sitemap.xml`：搜索引擎抓取入口。

预览页、实验页和“准备中”内容不应误进入正式导航、Sitemap 或公开索引。

## 八、本地开发与验证

网站使用 Node.js `>=22.13.0`。首次进入网站仓库：

```powershell
Set-Location -LiteralPath 'D:\codexguide_all\网站'
npm install
npm run dev
```

默认开发地址为 `http://localhost:3000`。如果端口被占用，使用其他端口，并在最终记录中写明实际地址。

标准验证命令：

```powershell
npm run lint
npx tsc --noEmit
npm test
npm run build
git diff --check
```

`npm test` 会先构建，再执行 `tests/rendered-html.test.mjs`。它主要检查公开路由、文章渲染、导航、图片和索引内容。涉及网页交互、响应式布局或主题时，还要用浏览器检查桌面和窄屏：

- 首页、目录页、文章页和专题页是否返回 200。
- 390px 左右窄屏是否横向溢出。
- 图片是否请求同源静态资源并返回正确 MIME。
- 搜索、目录、分页、回顶部和主题切换是否可用。
- 文章首屏、正文末尾和移动端导航是否发生遮挡。
- `prefers-reduced-motion` 下是否有合理降级。

不要把 `.env*`、`.next/`、`dist/`、`.wrangler/`、构建缓存、截图临时目录或本机凭据提交到仓库。

## 九、生产部署

正式发布路径是网站仓库的 `.github/workflows/deploy-cloudflare.yml`。推送 `main` 或手动触发工作流后，流程会：

1. 使用 Node.js 22.13.0。
2. 执行 `npm ci`。
3. 执行 `npm run lint`。
4. 执行 `npm test`，其中包含构建。
5. 使用生产 Cloudflare 凭据部署 Worker。
6. 检查 Bing/SEO 关键入口是否可访问。

生产环境不要把直接运行 `wrangler deploy` 得到的 `workers.dev` 地址当作正式发布结果。正式域名和 Worker 绑定由既有生产工作流维护。

### Cloudflare 域名管理

正式域名 `codexguide.io` 在 Cloudflare 中管理，Cloudflare 控制台截图已确认该域名入口。后续涉及 DNS、域名与 Worker 绑定、证书或生产路由时，先进入 Cloudflare 中的 `codexguide.io` 域名记录核对当前配置，再按既有生产发布流程操作。截图中的账号邮箱、登录资料、API Token 和其他凭据不写入交接文档或仓库。

部署后至少检查：

```text
https://codexguide.io/
https://codexguide.io/guides
https://codexguide.io/compare
https://codexguide.io/cases
https://codexguide.io/sitemap.xml
https://codexguide.io/llms.txt
```

如果只是内容修改，也要检查文章详情页、图片请求、canonical、文章分页和编辑入口。

## 十、SEO 和索引维护

网站已经包含 canonical、robots、Sitemap、结构化数据和 LLM 索引。新增或修改内容时同步检查：

- 公开页面是否允许索引。
- 页面 canonical 是否为 `https://codexguide.io` 下的规范地址。
- 草稿、预览和 upcoming 页面是否被排除。
- Sitemap 是否包含正式页面和文章，且不包含预览路径。
- `/llms.txt` 与 `/llms-full.txt` 是否包含准确链接。
- 重要页面是否存在稳定的标题、摘要、FAQ 或文章结构化数据。

### IndexNow

生产 Worker 使用两个变量或 Secret：

- `INDEXNOW_KEY`
- `INDEXNOW_SUBMIT_SECRET`

提交脚本需要设置：

```powershell
$env:INDEXNOW_ENDPOINT = 'https://codexguide.io/api/indexnow'
$env:INDEXNOW_SECRET = '<Cloudflare 中的 INDEXNOW_SUBMIT_SECRET>'
node scripts/submit-indexnow.mjs --sitemap https://codexguide.io/sitemap.xml
```

也可以直接传入一个或多个页面 URL。脚本会去重，并按每批最多 10,000 个 URL 提交。IndexNow 只是更新通知，不能代替 Bing Webmaster Tools 中的 Sitemap 提交。

发布后运行：

```powershell
npm run seo:bing
```

该检查会验证关键页面、robots、Sitemap、canonical、Bing 验证标签和 noindex 状态。

## 十一、Git、分支和交付边界

开始任何修改前先检查：

```powershell
git status --short --branch
git diff --stat
git diff --check
```

工作区有既有改动时，不要使用 `git add .`，也不要 reset、checkout 或删除不属于本次任务的文件。只暂存本次任务对应的文章、图片、内容数据或代码。

复杂任务或需要并行时，使用独立分支或 Worktree。每个任务都应有清楚的目标、修改范围、验证命令和回滚点。

提交前检查：

```powershell
git diff --cached --stat
git diff --cached
git diff --cached --check
```

提交信息应说明一个具体结果，例如：

```text
docs: 发布 Git 协作教程
fix(content): 修复文章渲染残留
feat(guides): 新增故障排查专题
```

教程仓库和网站仓库必须分别提交、分别推送。不能把教程正文、网站内容数据和无关临时文件混在同一个提交里。

## 十二、当前工作区状态

截至 2026-09-04，本地现场不是干净状态，接手时先重新确认，不要把下面状态直接当成远端状态：

- 教程仓库当前分支为 `main`，本地 HEAD 为 `40b3949`，相对 `origin/main` 显示 `[ahead 7, behind 60]`，并存在大量已修改和未跟踪文件。
- 网站仓库当前分支为 `fix/live-article-render-residue`，本地和远端均指向提交 `5707d87`，工作区存在 `TODO.md` 改动以及临时目录、构建缓存等未跟踪内容。
- 因为两个仓库都存在脏工作区，且教程仓库与远端分叉，本次交接没有自动拉取、合并、提交或推送。

接手后的第一步：分别阅读两个仓库的 `git status --short --branch`、`git log --oneline -8` 和 `git remote -v`，再决定是保留当前工作、建立隔离 Worktree，还是由负责人先处理分叉。

## 十三、常见问题处理顺序

### 网站页面没有更新

先确认教程仓库终稿是否已推送，再确认网站 `content/guides.ts` 或对应数据文件是否同步，最后检查图片是否复制到 `public/tutorial-images/`。不要只改教程仓库就期待网站自动更新。

### 文章图片 404

检查 Markdown 图片路径、网站静态目录、大小写和构建产物。网站文章优先使用同源 `/tutorial-images/...` 资源，不要依赖未登录访客无法访问的 GitHub 私有或原始路径。

### 文章出现在目录但不应该公开

检查 `content/sections.ts`、`content/guides.ts` 的状态筛选、`position`、搜索索引、Sitemap 和 LLM 索引。草稿不能只从目录删除，还要从所有公开索引中排除。

### 构建通过但页面仍有问题

构建只能证明代码可以打包，不能证明排版、交互和手机端没有问题。补做目标路由的 HTTP 检查、浏览器截图、关键元素定位、横向溢出检查和图片请求检查。

### Cloudflare 发布成功但正式域名不对

确认使用的是既有 GitHub Actions 生产流程，并访问 `https://codexguide.io`。不要用临时 `workers.dev` 地址替代正式域名验证。

### 外部平台或凭据失败

不把 Token、Cookie、完整错误上下文或账号信息写入正文和公开仓库。记录失败时间、错误类型、是否重试、替代方案和当前未完成状态即可。

## 十四、接手检查清单

### 内容侧

- [ ] 已读教程仓库 `README.md`、`贡献指南.md`、`TODO.md` 和目标栏目 README。
- [ ] 已确认本次处理的是正式文章，不是未发布稿、备份或素材工作区。
- [ ] 已核对官方来源、版本和本地实操环境。
- [ ] 已检查图片、图注、链接和敏感信息。
- [ ] 已区分正文、图片备份、处理报告和发布素材。

### 网站侧

- [ ] 已确认网站文章的 `slug`、`position`、作者、日期和 `sourcePath`。
- [ ] 已同步网站正文和实际使用图片。
- [ ] 已检查搜索、目录、分页、Sitemap、LLM 索引和 canonical。
- [ ] 已运行 lint、类型检查、测试、构建和 `git diff --check`。
- [ ] 已检查桌面与移动端页面。

### 发布侧

- [ ] 教程仓库和网站仓库的差异范围清楚。
- [ ] 只暂存本次任务相关文件。
- [ ] 已确认教程远端提交，再同步网站。
- [ ] 已通过网站仓库的生产发布流程。
- [ ] 已检查正式域名、关键页面和 SEO 入口。
- [ ] 已在 TODO 和当日工作记录中留下可复核信息。

## 十五、公众号发布

<!-- 本章节留空，由本人补充。 -->

## 十六、后续维护建议

后续维护优先处理三类事情：

1. 保持教程源和网站展示的一致性，减少同一事实在多个文件中重复维护。
2. 为高时效内容补充版本日期和复查节点，尤其是模型、价格、权限、安装命令和平台支持。
3. 将已经重复且稳定的检查沉淀为脚本或测试，但不要在流程尚未跑通前过早自动化。

每次交接或阶段性收尾，都要写清楚“已经验证到哪里、没有验证什么、下一步由谁完成”。这比只写“已完成”更有用，也能避免后续把本地修改、远端提交、生产部署和公开页面混为一个状态。
